# EDA Using libraries.

The file contain the python code where a web application is created using Streamlit. The code contains libraries like Profile Reporting, Sweetviz, Data Prep which gives the overall details about the dataset.
